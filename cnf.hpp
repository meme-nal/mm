#ifndef CNF_HPP
#define CNF_HPP

namespace mm {
  using Vector = float*;
  using Matrix = float**;
  using Tensor = float***;

  namespace net {
    namespace acts {}
  } // namespace net
} // namespace net

#endif // CNF_HPP